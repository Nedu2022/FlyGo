/**
 * Page gutter shared by the hero and every section under it. The design puts
 * content ~2.6% in from each edge, so the sections line up with the hero card
 * rather than sitting in a narrower 1200px column.
 */
export const PAGE = "mx-auto w-full max-w-[1720px] px-4 sm:px-[2.2%]";
